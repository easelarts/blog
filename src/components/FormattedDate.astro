---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-light-primary dark:text-dark-primary" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-uk', {
			day: 'numeric',
			month: 'short',
			year: 'numeric',
		})
	}
</time>
